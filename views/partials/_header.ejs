<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href="/maps">Rosette</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav justify-content-left">
        <% if (id) { %>
          <a class="nav-item nav-link" href="/maps/profile/favourites">My Favourites</a>
          <a class="nav-item nav-link" href="/maps/profile/contributions">My Contributions</a>
        <%}%>
      </div>
      <div class="navbar-nav d-sm-block d-md-none text-light">
        <% if (id) { %>
          <form action="/users/logout" method="POST">
            <div class="form-group mb-2">
              <p class="text-light">Logged In As: <%= name %></label>
              <button class="btn btn-outline-danger">Logout</button>
            </div>
          </form>
          <%} else { %>
            <a class="nav-item nav-link " href="/users/login">Login</a>
            <a class="nav-item nav-link" href="/users/register">Register</a>
            <% } %>
      </div>

    </div>
    <div class="navbar-nav d-none d-md-block">
      <% if (id) { %>
        <form action="/logout" method="POST">
          <div class="form-group mb-2">
            <button class="btn btn-outline-danger">Logout</button>
          </div>
        </form>
        <%} else { %>
          <a class="nav-item nav-link" href="/users/login">Login</a>
          <a class="nav-item nav-link" href="/users/register">Register</a>
  <% } %>
    </div>
  </nav>
</header>
